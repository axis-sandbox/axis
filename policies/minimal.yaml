version: 1
name: minimal-sandbox

filesystem:
  read_only:
    - /usr
    - /lib
    - /lib64
  read_write:
    - "{workspace}"

process:
  max_processes: 16
  max_memory_mb: 4096
  cpu_rate_percent: 50

network:
  mode: block
