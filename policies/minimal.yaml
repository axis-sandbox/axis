version: 1
name: minimal-sandbox

filesystem:
  read_only:
    - /usr
    - /lib
    - /lib64
  read_write:
    - "{workspace}"

process:
  max_processes: 0
  max_memory_mb: 0
  cpu_rate_percent: 0

network:
  mode: block
